import 'package:flutter/material.dart';

class AuthenticatePhoneCode extends StatelessWidget {
  const AuthenticatePhoneCode({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class AuthenticatePhoneTextField extends StatelessWidget {
  const AuthenticatePhoneTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
